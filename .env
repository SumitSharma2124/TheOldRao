
# Environment configuration
# Adjust values as needed before production deployment

NODE_ENV=development
PORT=3000

# Session secret (change this to a strong unique value in production)
SESSION_SECRET=558172b0e5158db6878782dbff00a93b0c773a2d5beb4be98f9ba0c76272d26d

# Mongo connection string (local dev)
# Using local MongoDB server now that it's running
MONGO_URL=mongodb://localhost:27017/theoldrao

# (Optional) Upload directory override
# UPLOAD_DIR=public/uploads/menu

# (Optional) Debug: relax TLS verification if local SSL interception causes Atlas handshake errors
# Set to true only temporarily for local debugging.
MONGO_TLS_INSECURE=false
